version: '3.8'

services:
  svelte-app:
    build: .
    ports:
      - "5000:5000"
    container_name: svelte-app
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
    environment:
      NODE_ENV: development